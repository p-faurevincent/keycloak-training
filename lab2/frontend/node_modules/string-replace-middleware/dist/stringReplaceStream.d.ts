/// <reference types="node" />
import { Transform } from 'stream';
declare type Options = {
    encoding: BufferEncoding;
    ignoreCase: boolean;
};
export default function StringReplaceStream(replacements: Record<string, string>, options?: Partial<Options>): Transform;
export {};
